# AI 에이전트 작업 지침

이 파일은 저장소 전체에 적용됩니다. 하위 디렉터리에 별도의 `AGENTS.md`가 있으면 해당 디렉터리에서는 더 가까운 지침을 함께 따릅니다.

## 작업 전 필수 확인

1. 사용자 요청과 관련 파일을 읽고 변경 범위를 정합니다.
2. `git status --short`와 `git diff`로 기존 변경을 확인합니다.
3. 기존 변경은 사용자 또는 다른 에이전트의 작업일 수 있으므로 임의로 되돌리거나 덮어쓰지 않습니다.
4. 불명확한 사항은 저장소의 코드와 문서를 먼저 조사합니다. 결과를 크게 바꿀 선택만 사용자에게 확인합니다.

## Unity 프로젝트 규칙

- 기준 Unity 버전은 `ProjectSettings/ProjectVersion.txt`의 `6000.3.13f1`입니다.
- 게임 파일은 `Assets/`, 패키지 의존성은 `Packages/`, 공용 프로젝트 설정은 `ProjectSettings/`에서 관리합니다.
- Unity 에셋을 추가·이동·삭제할 때 대응하는 `.meta` 파일도 반드시 함께 처리합니다. GUID가 불필요하게 바뀌지 않도록 기존 `.meta` 파일을 보존합니다.
- 씬, 프리팹, ScriptableObject 같은 직렬화 파일을 직접 편집할 때는 참조와 YAML 구조가 손상되지 않았는지 확인합니다.
- `Library/`, `Temp/`, `Obj/`, `Logs/`, `UserSettings/`, `.vs/`, 생성된 `*.csproj` 및 `*.sln`은 커밋하지 않습니다.
- 패키지를 변경하면 `Packages/manifest.json`과 `Packages/packages-lock.json`의 변경을 함께 검토합니다.
- 텍스트 기반 소스와 설정에는 UTF-8 및 저장소의 기존 줄바꿈 형식을 유지합니다.

## 구현 및 검증 원칙

- 한 작업은 하나의 명확한 목적에 집중하고, 요청과 무관한 정리는 섞지 않습니다.
- 새 동작에는 가능한 범위에서 Edit Mode 또는 Play Mode 테스트를 추가합니다.
- C# 변경 후에는 컴파일 오류를 확인하고, 가능하면 관련 테스트를 실행합니다.
- 씬·프리팹·에셋 변경은 Unity에서 열기, 콘솔 오류 확인, 필요한 플레이 동작 확인 등 변경 성격에 맞게 검증합니다.
- Unity Editor를 실행할 수 없어 검증하지 못한 경우, 완료 보고와 커밋 본문에 미검증 항목을 명시합니다.
- 작업 완료 전 `git diff --check`와 `git status --short`로 누락, 생성 파일, 의도하지 않은 변경을 확인합니다.

## 커밋 절차

사용자가 커밋을 요청했을 때만 커밋합니다. 커밋 전에는 변경 파일을 검토하고, 검증 결과를 확인하며, 현재 작업에 속한 파일만 스테이징합니다.

### 제목 형식

```text
<이모지>[<라벨>] <요약>
```

- 라벨은 파일 종류가 아니라 **작업 목적**을 기준으로 하나만 선택합니다.
- 요약은 변경 목적이 분명한 짧은 한국어 명령형 문장으로 작성합니다.
- 제목 끝에 마침표를 붙이지 않습니다.
- 제목만으로 충분하지 않으면 빈 줄 뒤에 본문을 작성합니다.
- 본문에는 변경 이유와 핵심 구현 내용을 적습니다. 검증하지 못한 사항이 있으면 함께 기록합니다.

### 라벨 선택표

| 라벨 | 선택 기준 | 작성 규칙 |
| --- | --- | --- |
| ✨`[Feat]` | 새로운 게임 시스템, 메카닉, 콘텐츠 추가 | 제목에 기능 설명 |
| ⚙️`[Core]` | 백엔드, 프레임워크, 도구, 시스템 기반 작업 | 제목에 기능 설명 |
| 🐛`[Fix]` | 명백한 버그 또는 기획 의도와 다른 동작 수정 | 제목에 현상, 본문에 수정 방법 |
| ♻️`[Refactor]` | 기능 변경 없이 코드 구조만 개선 | 제목을 `~ 구조 개선` 형태로 작성 |
| 🗺️`[Design]` | 레벨 디자인, 밸런싱, 수치 조정 | 제목에 현상, 본문에 조정 방법 |
| 🩹`[Patch]` | 사소한 프리팹·씬 변경, 오타 수정, 단순 수치 조정 | 제목에 현상, 본문에 수정 방법 |
| 🗃️`[Update]` | 데이터베이스 관련 업데이트 | 변경한 데이터와 목적 설명 |
| 🎨`[Asset]` | 비주얼 에셋, 사운드, 애니메이션 추가 및 수정 | 대상 에셋 설명 |
| 📝`[Docs]` | README, 기획서, 주석 등 문서 수정 | 대상 문서와 목적 설명 |
| 🧹`[Chore]` | 빌드 설정, 패키지 관리, Git 설정, 파일 이동, 폴더 구조 변경 | 유지보수 작업 설명 |
| 🔀`[Merge]` | 브랜치 병합 | 병합 대상 설명 |
| 🔥`[Del]` | 기능 제거 | 제거 대상 설명 |
| 🎉`[Init]` | 프로젝트 초기 구성 | 초기화한 범위 설명 |
| 💚`[Build]` | 빌드 파일 생성 | 제목에 버전 표기 |

### 커밋 예시

```text
✨[Feat] 플레이어 점프 기능 추가

🐛[Fix] 피격 후 플레이어가 움직이지 않는 현상 수정

피격 상태가 끝날 때 이동 잠금을 해제하도록 상태 전환을 수정함.

♻️[Refactor] 전투 상태 머신 구조 개선

💚[Build] v0.1.0 Windows 빌드 생성
```

여러 목적이 섞였다면 목적별로 커밋을 나눕니다. 단, 서로 분리하면 빌드가 깨지는 하나의 원자적 변경은 대표 목적의 라벨을 사용합니다.

## 완료 보고

- 변경한 내용, 수행한 검증, 남은 위험 또는 미검증 항목을 간결하게 알립니다.
- 커밋했다면 커밋 해시와 제목을 함께 알립니다.
